{% extends 'entity/base_index.html.twig' %}


{% block tabEntity %}
    <thead>
        <tr>
            <th>Id poste</th>
            <th>nom</th>
        </tr>
    </thead>
    <tbody>
    {% for p in postes %}
        <tr>
            <td>{{ p.idposte }}</td>
            <td>{{ p.nom }} </td>
            <td>
                <a class="btn btn-primary" href="{{ path(routes['update'],{id :p.idposte}) }}" role="button">Modifier</a>
                <a class="btn btn-primary" href="{{path(routes['delete'],{id:p.idposte}) }}" role="button">Supprimer</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
{% endblock %}

{% block actionEntity %}
    <div class="text-center">
        <span class="text-center">Nombre de postes : {{ sum }}</span>
        <a class="btn btn-primary ml-3 mb-3" href="{{ path(routes['insert']) }}" role="button">Créer un poste</a>
    </div>
{% endblock %}