{% extends 'entity/base_show.html.twig' %}

{% block tab %}
    <thead>
        <tr>
            <th>Nom</th>
            <th>Prenom</th>
            <th>Role</th>
            <th>Equipe</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ membre_personnel.nom }}</td>
            <td>{{ membre_personnel.prenom }} </td>
            <td>{{ membre_personnel.role }}</td>
            <td>{{ membre_personnel.equipe.idequipe }}</td>
        </tr>
    </tbody>
{% endblock %}

{% block previous %}
    <div class="text-center">
        <a class="btn btn-primary ml-3 mb-3" href="{{ path(routeIndex) }}" role="button">Retour à la liste des membres personnels</a>
    </div>
{% endblock %}

